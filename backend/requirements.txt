# Web Framework & Server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Database & ORM
sqlmodel>=0.0.14
alembic>=1.12.0          # DB 마이그레이션 관리 도구
asyncpg>=0.28.0          # PostgreSQL 비동기 드라이버 (고성능)
psycopg2-binary>=2.9.7   # 동기 처리 및 Alembic용 드라이버

# Cache & Lock (Redis)
redis>=5.0.0             # Redis 비동기 클라이언트 지원

# Auth & Security
pyjwt>=2.8.0             # JWT 토큰 생성 및 검증
bcrypt>=4.0.1            # 비밀번호 해싱 (passlib+bcrypt 4.1+ 호환 이슈로 직접 사용)
python-multipart>=0.0.6  # OAuth2 폼 데이터 파싱

# Config & Validation
pydantic-settings>=2.0.0 # .env 환경변수 관리
email-validator>=2.1.0   # Pydantic EmailStr
httpx>=0.27.0            # Google OAuth 토큰·userinfo